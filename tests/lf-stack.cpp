#include "../include/lock_free_allocator/lf_stack.h"
#include "../include/lock_free_allocator/slab.h"

int main(void)
{
    //lock_free_stack<Slab*> free_slabs;
    //free_slabs.push_front(new Slab(16));
    //auto slab = free_slabs.pop_front();
    //(*slab)->insert();
    return 0;
}
